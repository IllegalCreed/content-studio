---
name: review-and-handoff
description: 发布协作与人工接管：建立发布计划、准备人工确认包、确认接管并晋升产物。当内容需要审核、发布前确认、等待人工输入或产物需要晋升为项目素材时使用。
---

# 发布协作与人工接管

## 1. 建立发布计划

调用 `create_publication_plan` 为活动内渠道成品建立本地发布安排和发布任务。
此工具不会执行渠道发布。

## 2. 准备人工确认包

需要人工确认时调用 `create_owner_handoff`，只保存校验和、清单和官方页面地址，
不保存任何凭据。

## 3. 等待并展示确认

任务进入 `input_required` 后，把确认包展示给用户并等待明确确认。等待期间不要
继续录制或执行后续步骤。

## 4. 确认接管

用户明确确认后调用 `confirm_owner_takeover`，任务回到录制并继续同一会话。
只接受确认动作，不接受任何凭据输入。

## 5. 晋升产物

用户明确选择后调用 `promote_activity_artifact`，把活动产物登记为项目素材；
不删除原产物。

## 边界

- 绝不自动发布、绝不绕过人工确认；
- 绝不收集或记录密码、Cookie、Token；
- 发布回执和渠道状态以 `marketing-ops` 快照为准，本地状态不能授予或扩大发布
  权限。
